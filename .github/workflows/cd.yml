name: Deployment

on:
  push:
    branches:
      - main
permissions:
  issues: write
jobs:
  dev:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: deploy
        run: |-
          echo "dev"
  prod:
    runs-on: ubuntu-latest
    steps:
      - uses: trstringer/manual-approval@v1
        with:
          secret: ${{ github.TOKEN }}
          approvers: ivancorniel
      - name: deploy
        run: |
          echo "deplying"
